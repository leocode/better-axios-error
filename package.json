{
  "name": "better-axios-error",
  "version": "2.0.0",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "license": "MIT",
  "scripts": {
    "test": "yarn build && tape tests/**/*.spec.js",
    "prepublish": "yarn build",
    "build": "tsc"
  },
  "peerDependencies": {
    "axios": "^0.24.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.12",
    "axios": "^0.24.0",
    "tape": "^5.0.1",
    "typescript": "^4.5.4"
  }
}
